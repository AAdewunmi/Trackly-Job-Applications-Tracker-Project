"""Roles application configuration."""

from django.apps import AppConfig


class RolesConfig(AppConfig):
    """Configure the roles app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.roles"
