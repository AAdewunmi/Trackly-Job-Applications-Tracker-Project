# Local Setup

## Purpose

This document describes how to run Trackly locally using Docker Compose and PostgreSQL.

## Requirements

Install:

- Docker
- Docker Compose

No local Python or PostgreSQL installation is required when using the container workflow.

## Environment File

Create a local environment file:

```bash
cp .env.example .env